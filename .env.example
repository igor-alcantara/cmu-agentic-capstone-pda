# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=
# Sonnet-class model per the design docs. Any current Claude model id works.
PDA_MODEL=claude-sonnet-5
# 1 = run every agent against the deterministic MockLLM (no key, no network)
PDA_MOCK=0
